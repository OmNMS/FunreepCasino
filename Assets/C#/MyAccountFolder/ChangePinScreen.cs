using UnityEngine.UI;
using UnityEngine;
using Com.BigWin.Frontend.Data;

public class ChangePinScreen : MonoBehaviour
{
    public InputField pinInputField;
    public InputField newPinInputField;
    public InputField confirmPinInputField;
    public Button okBtn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
